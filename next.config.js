// import type { NextConfig } from "next";

// const nextConfig: NextConfig = {
//   /* config options here */
// };

// export default nextConfig;

module.exports = {
  // Tells Next.js to look for the pages folder inside the src directory
  experimental: {
    srcDir: "pages", // This tells Next.js to look in the src directory for pages
  },
};
